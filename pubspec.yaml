name: fractions
description: Simple math fractions library. Library create simple fraction and define basic math operations.
version: 0.0.3
homepage: https://github.com/adamjak/dart_fractions

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dart_numerics: ^0.0.5


dev_dependencies:
  flutter_test:
    sdk: flutter
